contextualize_data <- function(dataset, contextual_df, contextuals = c("region", "pop",
                                                                       "lat/lng", "area",
                                                                       "elevation", "initiatives")){
  # Function to append contextual data to the dataset
  
  
}